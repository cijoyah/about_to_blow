<div id="form">
  <div class="wrapper_skinny">

    <h1>new</h1>

    <%= form_for ([ @user, @playlist ]) do |f| %>
      <%= f.label :title %></br>
      <%= f.text_field :title %>

      <br>

      <%= f.label :description %></br>
      <%= f.text_area :description %>
    
      <br>
      <br>

      <%= f.submit %>

    <% end %> 

  </div>
</div>